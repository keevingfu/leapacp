{
  "buildCommand": "cd frontend && npm install && mv tsconfig.json tsconfig.json.bak && npm run build && mv tsconfig.json.bak tsconfig.json || true",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install --prefix frontend",
  "devCommand": "cd frontend && npm run dev",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
